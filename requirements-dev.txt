# Testing & development dependencies
pytest>=8.0.0
pytest-cov>=5.0.0
boto3>=1.34.0
boto3-stubs[s3,dynamodb,bedrock-runtime]>=1.34.0
moto[s3,dynamodb]>=5.0.0

# CDK (needed for `cdk synth` in local dev — also listed in infrastructure/requirements.txt)
aws-cdk-lib>=2.258.0
constructs>=10.0.0
